
// import qaData from "../data/stackNest_qa.json"


// type QaType = {
//   question: string;
//   answer: string;
// };

// export const findAnswers = (useQuery: string) => {
//   const query = useQuery.toLowerCase();
//   const matchedQA: QaType = qaData.find(
//     (qa: QaType) =>
//       qa.question.toLowerCase().includes(query) ||
//       query.includes(qa.question.toLowerCase())
//   );

//   return matchedQA?.answer || null;
// };
